@if(teamDTO is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <TeamsForm @ref="teamForm" TeamDTO="teamDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
